const routes = {
    HOME: '/',
    LOGIN: '/login',
    ADD_PRODUCTS: '/add',
    EDIT_PRODUCTS: '/edit/:id',
    REGISTER: '/register',
    DETAILS: '/details/:id',
    USER: '/user',
}

export default Object.freeze(routes);